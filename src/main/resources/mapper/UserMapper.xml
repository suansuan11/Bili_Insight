<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecut.bili_insight.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.ecut.bili_insight.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password_hash"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="biliSessdata" column="bili_sessdata"/>
        <result property="biliJct" column="bili_jct"/>
        <result property="biliBuvid3" column="bili_buvid3"/>
        <result property="biliLoginAt" column="bili_login_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password_hash, email, role, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{role}, NOW(), NOW())
    </insert>

    <update id="updateBiliCredential">
        UPDATE `user`
        SET bili_sessdata = #{sessdata},
            bili_jct      = #{jct},
            bili_buvid3   = #{buvid3},
            bili_login_at = NOW(),
            updated_at    = NOW()
        WHERE id = #{userId}
    </update>

</mapper>
